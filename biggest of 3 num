#include<stdio.h>
#include<conio.h>
main()
{
	int a,b,c;
	printf("enter the values of a,b,c\n");
	scanf("%d%d%d",&a,&b,&c);
	if(a>b)
	{
		if(a>c)
		{
			printf("a is big");
		}
		else
		{
			printf("c is big");
		}
	}
	else
	{
		if(b>c)
		{
			printf("b is big");
		}
		else
		{
			printf("c is big");
		}
	}
}
